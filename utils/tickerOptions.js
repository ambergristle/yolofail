// const url =
//   "https://api.marketstack.com/v1/tickers?access_key=YOUR_ACCESS_KEY&limit=1000&offset=OFFSET";
//
// const tickers = rawTickers.reduce((areValid, { symbol, has_eod }) => {
//   if (has_eod) areValid.push({ symbol, has_eod });
//   return areValid;
// }, []);
